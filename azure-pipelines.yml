trigger:
- main  # Se ejecuta cuando hay cambios en la rama 'main'

pool:
  name: Default  # Nombre del pool de agentes auto-hospedados
  demands: []  # Asegúrate de usar el nombre correcto de tu agente

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '18.x'  # Usa Node.js 18 para compilar React

- script: |
    echo "📦 Instalando dependencias..."
    npm install
  displayName: 'Instalar dependencias y construir la app'
  
- script: |
    echo "⚡ Generando build de React..."
    npm run build
  displayName: 'Generar build de React'

- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: |
      Write-Host "🔍 Verificando estructura de archivos antes de subir a S3..."
      Get-ChildItem -Path ./dist -Recurse  # Muestra archivos de la carpeta dist

      Write-Host "🔐 Configurando credenciales de AWS..."
      [System.Environment]::SetEnvironmentVariable("AWS_ACCESS_KEY_ID", "$(AWS_ACCESS_KEY_ID)", "Process")
      [System.Environment]::SetEnvironmentVariable("AWS_SECRET_ACCESS_KEY", "$(AWS_SECRET_ACCESS_KEY)", "Process")

      Write-Host "☁️ Subiendo archivos a S3..."
      & "C:\Program Files\Amazon\AWSCLIV2\aws.exe" s3 sync ./dist s3://reto-aws --delete --region us-east-2 --exact-timestamps

      Write-Host "🌀 Limpiando la caché de CloudFront..."
      & "C:\Program Files\Amazon\AWSCLIV2\aws.exe" cloudfront create-invalidation --distribution-id E19AWFGK4NV1FF --paths "/*"
  displayName: 'Desplegar en S3 y limpiar la caché de CloudFront'